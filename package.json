{
  "name": "cae",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "lint": "eslint src --ext .ts,.tsx,.js,.jsx",
    "format": "prettier --write src/**/*.{ts,tsx,js,scss}",
    "deploy": "yarn run build && git add -f dist && git commit -m'hosting'  && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "dist": "^0.1.2",
    "got": "^12.3.1",
    "npm-check-updates": "^16.0.5",
    "ress": "^4.0.0",
    "uglify": "^0.1.5"
  },
  "devDependencies": {
    "@types/node": "^16.9.1",
    "autoprefixer": "^10.4.7",
    "eslint": "^8.19.0",
    "prettier": "^2.7.1",
    "sass": "^1.53.0",
    "typescript": "^4.7.4",
    "vite": "^2.5.5"
  }
}
